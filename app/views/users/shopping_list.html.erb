<h1>Shopping List</h1>
<h2> Amount of food item to buy: <%= @total_item %></h2>
<h2> Total price: $<%= @total_price %></h2>
<table>
  <tr>
    <th>Food</th>
    <th>Quantity</th>
    <th>Price</th>
  </tr>
  <% @shopping_list.each do |food| %>
  <tr>
    <td><%= food.name %></td>
    <td><%= food.quantity %> <%= food.measurement_unit%></td>
    <td>$<%= food.price * food.quantity %></td>
  </tr>
  <% end %>
</table>
