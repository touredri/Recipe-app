<%= form_with(model: [@recipe, @recipe_food], method: :post, local: true, class: 'recipe_form') do |form| %>
  <% if @recipe_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@recipe_food.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
        <% @recipe_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :recipe_id, value: @recipe.id %>

  <div>
    <%= form.label :food_name %><br/>
    <%= form.collection_select :food_id, @foods, :id, :name %>
  </div>

  <div>
    <%= form.label :ingredient_quantity %><br/>
    <%= form.text_field :quantity %>
  </div>

  <div>
    <%= form.submit 'Add Ingredient' %>
  </div>
<% end %>
